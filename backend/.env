# MySQL 连接串（部署到服务器后按实际地址修改）
DATABASE_URL="mysql+pymysql://fintech_user:CHANGE_ME@127.0.0.1:3306/local_fintech?charset=utf8mb4"

# 后端监听配置
HOST="0.0.0.0"
PORT="8000"
UVICORN_WORKERS="1"
UVICORN_RELOAD="false"

# 对外访问地址（用于脚本输出提示）
PUBLIC_BASE_URL="http://YOUR_SERVER_IP_OR_DOMAIN:8000"
